# Applications

The planned `web` application will be a React/TypeScript/Vite static-first client. Do not scaffold it until the bootstrap issue records exact tool versions, Pages base path, and the minimal Gate A fixture.

