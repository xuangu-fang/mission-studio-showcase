# Public Packages

Planned logical packages:

- `contracts`: public JSON Schema and validation;
- `domain`: event ordering, playback, projections, comparison;
- `cesium-adapter`: renderer and camera lifecycle;
- `ui`: design tokens and domain components.

Start with the smallest structure that preserves dependency direction; avoid monorepo ceremony without real artifacts.

